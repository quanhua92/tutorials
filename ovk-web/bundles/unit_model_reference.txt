# unit_model — web provenance log
# Every URL has a "Verifies:" line stating exactly what it confirms.
# STEP 2 of HOW_TO_RESEARCH.md is non-skippable; ≥2 sources per concept.

https://hyperframes.heygen.com/packages/cli
  Verifies: HyperFrames CLI is the renderer; the workspace layout (index.html root host + per-slide compositions + assets/) is HF's native on-disk format, which is why "the editable source files ARE HyperFrames' files" (RFC §1, §10).

https://developer.mozilla.org/en-US/docs/Learn/Getting_started_with_the_web/Dealing_with_files
  Verifies: the "one folder = one unit, disambiguated by folder not filename" convention mirrors standard static-site/Next.js file-routing practice (RFC §5.5 "matches filesystem-site conventions").

(in-repo) docs/rfc-0001.md §5.1–§5.6
  Verifies: the symmetric unit structure (project root + each slide = a folder with index.html + index.json), the file-role split (root = HOST + canvas/theme/audio/ordering; slide = bare <template> + fields/assets/voiceover/duration), and the SHA-256 content-addressed assets/ directory.

(in-repo) docs/AGENTS.md "Template structure"
  Verifies: the on-disk layout the editor must emit at export (templates/<id>/index.html + layouts/ + assets/), which is the real-world instance of the abstract unit model.
