[project]
name = "slm-engineering"
version = "0.1.0"
description = "SLM Engineering concept bundles — runnable ground-truth + guide + interactive companion"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.12.1",
]
