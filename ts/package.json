{
  "name": "tutorials-ts",
  "version": "0.0.0",
  "private": true,
  "description": "The TypeScript Expertise Curriculum — concept-as-a-bundle (Node + V8 + libuv). Each bundle = {name}.ts (ground truth) + {name}_output.txt (captured stdout) + {NAME}.md (guide).",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "typecheck": "pnpm -r exec tsc --noEmit"
  }
}
