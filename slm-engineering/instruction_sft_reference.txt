Provenance log for instruction_sft.py / INSTRUCTION_SFT.md / instruction_sft.html
================================================================================
Every format, convention, default value, and API claim below was web-checked in
>=2 independent sources. Distinct URLs: 7.

The bundle verifies four things, each traced to the primary source + >=1 other
authoritative source:
  * The ChatML layout (<|im_start|>role\n{msg}<|im_end|>\n)        -> HF chat_templating + Gottesman
  * The -100 ignore_index DEFAULT in CrossEntropyLoss               -> PyTorch docs (verbatim signature)
  * The label-masking convention (labels=-100 on user/system)       -> Gottesman worked table + trl docs
  * completion-only / assistant-only loss (the SFT rule)            -> trl SFTTrainer + Gottesman

GOLD ANCHORS pinned and recomputed identically in the .py AND the .html:
  * The fixed 2-turn toy ChatML conversation has T = 24 toy tokens,
    A = 4 assistant (loss-bearing) tokens, masked = 20.
  * A = count of ROLE_ASSISTANT positions = 4 (two spans: content + closing <|im_end|>).
  * The .html reproduces the SAME ids + role arrays in JS, recomputes A and
    masked, and the [check: OK] badge asserts A == 4 and masked == 20.

================================================================================
[1] https://huggingface.co/docs/transformers/chat_templating
    HuggingFace Transformers documentation, "Chat templates" (authoritative;
    covers apply_chat_template, add_generation_prompt, model training).
    Verifies: the ChatML layout VERBATIM -- the docs render
      "<|im_start|>user\nHi there!<|im_end|>\n<|im_start|>assistant\n..."
    with one newline after <|im_start|>role and one newline after each <|im_end|>.
    Confirms roles {system, user, assistant}, that chat models are "still just
    language models that continue a sequence of tokens", that
    add_generation_prompt appends "<|im_start|>assistant\n" for inference, and
    that for training you set add_generation_prompt=False. Also confirms
    different models use different control tokens (Mistral [INST], Zephyr
    <|user|>/<|assistant|>, ChatML <|im_start|>/<|im_end|>) -- hence the chat
    template is required.

[2] https://yonigottesman.github.io/2024/05/13/mask-user-tokens.html
    Yoni Gottesman, "Mask Your User Tokens" (May 2024) -- the canonical
    "why mask the prompt" writeup with a full worked token-id table.
    Verifies: BOTH the ChatML format AND the -100 masking convention in one
    source. Shows the apply_chat_template output for ChatML:
      "<|im_start|>user\nText: ...?<|im_end|>\n<|im_start|>assistant\n...<|im_end|>\n"
    and a position-by-position table where labels = -100 on every user /
    <|im_start|> / role / newline / <|im_end|>-closer-of-non-assistant-turn and
    only the assistant content + its closing <|im_end|> keep their real id.
    States: "-100 is the default ignore_index in PyTorch's CrossEntropyLoss, so
    when the loss function compares the output tokens with the labels, it will
    ignore all tokens labeled -100." Demonstrates empirically that masking user
    tokens lowers validation loss vs naive labels=input_ids. Second-source
    confirmation of [1], [3], [5], [6].

[3] https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html
    PyTorch documentation, torch.nn.CrossEntropyLoss (authoritative).
    Verifies: the EXACT class signature with the DEFAULT ignore_index = -100:
      "class torch.nn.CrossEntropyLoss(weight=None, size_average=None,
       ignore_index=-100, reduce=None, reduction='mean', label_smoothing=0.0)"
    and "ignore_index (int, optional) -- Specifies a target value that is
    ignored and does not contribute to the input gradient. When size_average is
    True, the loss is averaged over non-ignored targets." This is why setting
    labels = -100 on non-assistant positions makes the loss skip them for free.
    The unreduced loss formula explicitly multiplies by the indicator
    1{y_n != ignore_index}, so masked positions contribute exactly 0.

[4] https://huggingface.co/docs/trl/sft_trainer
    HuggingFace TRL documentation, "SFT Trainer" (authoritative).
    Verifies: the SFT loss is "the token-level cross-entropy loss ...
    -sum_t log p(y_t | y_<t)"; "Padding tokens (if present) are ignored in the
    loss computation by applying an ignore index (default: -100)"; and the two
    shipped completion-only modes:
      * "completion_only_loss=True ... loss is computed only on the completion,
        ignoring the prompt tokens"
      * "assistant_only_loss=True ... loss is computed ONLY on the assistant
        responses, ignoring user or system messages"
      * (requires the chat template to include {% generation %} markers for
        assistant_only_loss; multi-turn is supported)
    Also confirms Qwen uses ChatML ("<|im_end|>" as the eos token for
    Qwen2.5-1.5B) -- ChatML is the canonical modern chat format.

[5] https://github.com/huggingface/trl/blob/main/trl/trainer/utils.py
    HuggingFace TRL source, DataCollatorForCompletionOnlyLM (the legacy
    completion-only collator; authoritative code).
    Verifies: the OLDER implementation of completion-only loss -- a collator
    that masks everything before the response_template (the assistant turn
    marker) to -100, so loss is computed only on the response. This is the
    pre-assistant_only_loss mechanism and the one cited by Gottesman [2]. The
    CURRENT trl (completion_only_loss / assistant_only_loss, [4]) supersedes it
    with the same masking semantics.

[6] https://community.openai.com/t/how-does-chatml-do-the-exact-formatting/80751
    OpenAI Developer Community, "How does ChatML do the exact formatting?"
    (the original ChatML spec discussion).
    Verifies: the canonical ChatML format published by OpenAI -- each message is
    "<|im_start|>{role}\n{content}<|im_end|>\n", where <|im_start|> and
    <|im_end|> are SPECIAL TOKENS (single tokens, not literal strings), the role
    and the newline are separate, and a "\n" follows every <|im_end|>. Confirms
    the layout that [1], [2], and [4] also render, traced to the original
    OpenAI ChatML definition.

[7] https://github.com/lm-sys/FastChat/blob/main/fastchat/train/train.py
    LM-Sys FastChat, train.py (authoritative open-source SFT trainer; cited by
    [2] as a script that DOES mask correctly).
    Verifies: a production multi-turn SFT mask implementation -- it walks the
    conversation, sets labels = -100 on user/system/separator tokens, and keeps
    the real id only on the assistant spans (content + turn-ending token). This
    is the reference implementation of the "completion-only / assistant-only"
    masking rule the bundle models, independent of HuggingFace's collator.

NOTABLE VERIFICATION NOTES:
  * The ChatML layout "<|im_start|>role\n{msg}<|im_end|>\n" is confirmed
    verbatim by [1] (HF docs), [2] (Gottesman, full token stream), [6] (OpenAI
    community, the original spec) -- three independent renderings that agree to
    the newline. Qwen / SmolLM / OpenAI ChatGPT all use this layout ([4], [6]).
  * ignore_index = -100 is the DEFAULT in torch.nn.CrossEntropyLoss, confirmed
    verbatim in the class signature [3]; the same -100 is the convention every
    SFT stack uses ([2], [4], [5], [7]).
  * The masking convention -- labels = -100 on system / user / role-markers /
    padding, real id on assistant content + closing <|im_end|> -- is confirmed
    by the worked table in [2], the trl SFTTrainer docs [4], the legacy
    DataCollatorForCompletionOnlyLM [5], and FastChat's train.py [7].
  * The <pad> handling pitfall (CrossEntropyLoss ignores the LABEL -100, NOT
    the token id 0; a collator that leaves pad labels as id 0 silently trains
    the model to predict <pad>) follows directly from [3] (ignore_index acts on
    the target value, not the vocab id) and [4] ("Padding tokens ... are ignored
    ... by applying an ignore index (default: -100)").

UNVERIFIED FACTS: none outstanding. The ChatML layout, the -100 default
ignore_index, the label-masking convention, and the completion-only /
assistant-only loss modes each trace to >=2 independent sources above.
